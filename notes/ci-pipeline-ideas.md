# CI Pipeline Ideas

- Add caching for pip and npm dependencies
- Use matrix builds for Python 3.11, 3.12
- Parallelize linting and unit tests
- Consider adding a deploy step for staging
- Explore using act for local GitHub Actions testing
